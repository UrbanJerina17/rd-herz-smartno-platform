"use client";import {defaults} from "./content";import type {SiteContent} from "./types";const KEY="herz-content-v1";export function load():SiteContent{if(typeof window==="undefined")return defaults;try{const x=localStorage.getItem(KEY);return x?JSON.parse(x):defaults}catch{return defaults}}export function save(x:SiteContent){localStorage.setItem(KEY,JSON.stringify(x));window.dispatchEvent(new Event("herz-change"))}export function reset(){localStorage.removeItem(KEY);window.dispatchEvent(new Event("herz-change"))}
